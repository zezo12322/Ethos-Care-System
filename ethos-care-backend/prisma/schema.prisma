generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres.wlyrzlahujmkcznqgnhj:1357z2468A%40%40@aws-1-eu-west-1.pooler.supabase.com:5432/postgres"
  directUrl = "postgresql://postgres.wlyrzlahujmkcznqgnhj:1357z2468A%40%40@aws-1-eu-west-1.pooler.supabase.com:5432/postgres"
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String
  name      String
  role      String   @default("CASE_WORKER")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  assignedCases Case[]
  actions CaseHistory[]
}

model Case {
  id                      String   @id @default(uuid())
  applicantName           String
  nationalId              String? 
  caseType                String   @default("غير محدد")
  
  // Legacy status
  status                  String   @default("قيد المراجعة")
  
  // New workflow fields
  lifecycleStatus         String   @default("DRAFT")
  completenessStatus      String   @default("MISSING_DOCUMENTS")
  decisionStatus          String   @default("PENDING_DECISION")
  
  priority                String   @default("NORMAL")
  location                String   @default("بني سويف")
  description             String?
  familyId                String?
  family                  Family?  @relation(fields: [familyId], references: [id])
  
  assignedToId            String?
  assignedTo              User?    @relation(fields: [assignedToId], references: [id])
  
  requiresCommitteeReview Boolean  @default(false)
  lastActionAt            DateTime @default(now())
  
  history                 CaseHistory[]
  
  createdAt               DateTime @default(now())
  updatedAt               DateTime @updatedAt
}

model CaseHistory {
  id                  String   @id @default(uuid())
  caseId              String
  case                Case     @relation(fields: [caseId], references: [id], onDelete: Cascade)
  fromLifecycleStatus String?
  toLifecycleStatus   String
  fromDecisionStatus  String?
  toDecisionStatus    String
  action              String
  reason              String?
  performedById       String?
  performedBy         User?    @relation(fields: [performedById], references: [id])
  metadataJson        String?
  performedAt         DateTime @default(now())
}

model Family {
  id             String   @id @default(uuid())
  headName       String
  nationalId     String?
  phone          String?
  socialStatus   String   @default("متزوج/ة")
  job            String?
  income         String?
  city           String   @default("بني سويف - المركز")
  village        String?
  addressDetails String?
  address        String?
  membersCount   Int      @default(1)
  status         String   @default("تحت التقييم")
  cases          Case[]
  lastVisit      DateTime @default(now())
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
}

model Operation {
  id         String   @id @default(uuid())
  name       String
  type       String
  date       DateTime @default(now())
  target     Int      @default(0)
  achieved   Int      @default(0)
  status     String   @default("تجهيز")
  progress   Int      @default(0)
  volunteers Int      @default(0)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}

model Partner {
  id        String   @id @default(uuid())
  name      String
  type      String   @default("مؤسسة مانحة")
  contact   String?
  email     String?
  status    String   @default("نشط")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Location {
  id        String   @id @default(uuid())
  name      String
  type      String   @default("قرية")
  region    String   @default("بني سويف")
  status    String   @default("مفعل")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
